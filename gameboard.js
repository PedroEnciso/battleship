const GameBoard = () => {};

module.exports = GameBoard;

// place ships at specific coordinates

// recieveAttack that takes coordinates and determines whether or not the attack hit a ship. calls hit function
// record the coordinates of missed shots

// keeps track of missed shots

// report if all ships have been sunk
